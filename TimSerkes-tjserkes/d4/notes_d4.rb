# hashes are looked up by their keys

numbers.keys more useful than numbers.values

numbers.has_key?

#modifying a hash works similar to an array
#ordering in a hash doesn't matter. so pushing doesn't
#really make sense. add and delete by key
#can't look up by value or delete. don't do it
#all CRUDS 